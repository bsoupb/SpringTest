<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.bsoupb.test.database.repository.StoreRepository">
 	
 	<select id="selectStoreList" resultType="com.bsoupb.test.database.domain.Store"> <!-- 메소드 이름, 파라미터, 리턴 타입 : 한 행을 저장하기 위한 (entity class)타입 -->
 			SELECT 
			`id`
			, `name`
			, `phoneNumber`
			, `address`
			, `businessNumber`
			, `introduce`
			, `createdAt`
			, `updatedAt`
		FROM 
			`store`
 	</select>
 
 </mapper>
 
 